<?xml version="1.0"?>
<launch>

  <arg name="cfg"    default="car" />

  <!-- RVIZ -->
  <node pkg="rviz" type="rviz" name="rviz" args="-d $(find car_gazebo_models)/cfg/rviz/$(arg cfg).rviz" />
  <param name="/use_sim_time" value="true"/>
</launch>
